CREATE TABLE computerParts (
	id INTEGER PRIMARY KEY,
	Product_Type TEXT,
	Quantity INTEGER,
	Price TEXT,
	Product_Name TEXT
);

CREATE TABLE GPUs (
	id INTEGER PRIMARY KEY,
	Product_Type TEXT,
	Quantity INTEGER,
	Price TEXT,
	Product_Name TEXT
);

CREATE TABLE products (
	Text Product_Type, 
	NUMBER Num_of_Items_Per_Product_Type
);